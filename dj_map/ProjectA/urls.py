from django.urls import path
from .views import MakeProductAPIView, show



urlpatterns = [
    # path('api/make/', MakeProductAPIView.as_view(), name='api-make'),
    # path('purchase/', show, name='purchase'),

]